// i18n Configuration for Admin Panel - Stub

export type Locale = 'en';

export const locales: Locale[] = ['en'];

export const localeNames: Record<Locale, string> = {
    en: 'English'
};

export const defaultLocale: Locale = 'en';
